image: gcc

# build the project
build:
  stage: build
  before_script:
    - apt update && apt -y install cmake
  script:
    - cmake -B Build
    - cmake --build Build
  artifacts:
    paths:
      - Build/IOTest 
      - Input

# run tests using the binary built before
test:
  stage: test
  script:
    - ./Build/IOTest # would like to do this using ctest to run all tests with a single command

# deploy:
#   stage: deploy
#   script: 
#     - ...
